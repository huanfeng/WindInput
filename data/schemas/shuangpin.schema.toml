[schema]
id = "shuangpin"
name = "双拼"
icon_label = "双"
version = "1.0"
author = "内置"
description = "双拼输入方案"

[engine]
type = "pinyin"

[engine.pinyin]
scheme = "shuangpin"

[engine.pinyin.shuangpin]
layout = "xiaohe"

# 辅助码：拼音候选的字形二次筛选（小鹤拆分码）。路径相对 schemas 目录。
# 词组逐字首码匹配为固定语义（第 i 位命中第 i 字任一码的首字符；短于词字数 = 前缀态保留）。
#
# ⚠️ 本段只声明「双拼推荐哪张辅助码表」，**不代表功能开启**。
# 总开关是 config.toml 的 [schema.pinyin.aux_code].enabled（出厂 false）；
# 用户要单独在双拼开启，写 %APPDATA%\WindInput\schema_overrides\shuangpin.toml：
#     [engine.aux_code]
#     enabled = true
# 行为字段（enabled / max_phrase_len）在此留空 = 回落全局，见 schema-config-layering.md §4。
[engine.aux_code]
files = ["aux_code/flypy_full.txt"]

# 反引号进辅助码（组码中有效；空缓冲时落普通标点）。覆盖全局 backtick = "temp_pinyin"
# 的默认绑定——temp_pinyin_target 对拼音方案本就不成立，反引号在此原为自由键
# （双拼不支持手动音节分隔符，见 pinyin_separator_key 的 pinyin_is_shuangpin 早退）。
# 绑定本身不激活功能：enabled = false 时门卫拒绝进入，该键照常落普通标点。
#
# ⚠️ 若将来双拼开放手动分隔符，本绑定会被分隔符臂夺走（它在按键裁决里排在 key_actions
# 之前）。届时在上方 [engine.pinyin] 段写 separator = "none" 即可把反引号还给辅助码
# ——这正是 separator 做成方案级可覆盖的原因。
[key_actions]
backtick = "aux_code"

[[dictionaries]]
id = "pinyin_main"
path = "pinyin/rime_frost.dict.yaml"
type = "rime_pinyin"
default = true
base_order = 0

# 权重不归一化——这是刻意的，勿加 [weight_spec]：
#   1. 拼音权重刻意在与码表不同的轴上（max 1537 万），其与码表的先后由协调器
#      source_tier 的档位隔离决定，不靠比 weight；
#   2. pinyin/mod.rs 的 COMPLETION_FAR_WEIGHT_FLOOR(100) 与
#      SENTENCE_YIELD_WEIGHT_FLOOR(50) 是**按原始权重分布标定的绝对阈值**（该处
#      代码注释亦有明示）。拼音 p50 仅 20，归一化后人人过线，两道闸门会静默失效。
# 本库分布（供设计者查阅）：median≈200 max≈1926 万 —— 亦是上述 FLOOR 取值的依据。
