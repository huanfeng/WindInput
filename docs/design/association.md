# 联想（Association）

上屏之后按**刚上屏的内容**给候选，而不是按编码。

## 0. 两种联想，回答的是不同的问题

这是本功能最重要的一个区分，也是设计过程中被纠正过一次的地方。

| | 上文怎么用 | 打完「中」给什么 | 平台默认 |
|---|---|---|---|
| **词语联想** `word` | 当**前缀** | 「中国」「中间」「中心」 | — |
| **智能联想** `smart` | 当**上下文** | 「，」「的」「国」 | 移动端 |
| 关 `off` | — | — | PC |

PC 输入法里说的「联想」几乎总是前者：词库里以刚上屏内容开头的更长的词，选中后补出
剩余部分。它**不需要任何上下文模型**，只需要一份带权重的词库。★ **不含标点**——
「中」后面接「，」在这个语义下根本不成立，它不是以「中」开头的词。

后者才是移动端刚需：软键盘上每多打一个字都很贵，所以要连标点带下一个词一起猜。

> ⚠️ **两者不是「弱版 / 强版」的关系，不能用一个开关的强弱表达。** 同样打完「中」，
> 一个该给「中国」，另一个该给「国」。混在一起出，用户会看到「中国」和「国」并排，
> 完全不知道选中哪个会得到什么。

一期最初只做了 `smart`，并把它当成「联想」的全部——真机上表现为**每打一个字都跳出
一串标点**，而 PC 用户要的根本是另一件事（2026-08-16 用户反馈）。

## 1. 它与普通候选的根本差别

| | 普通候选 | 联想候选 |
|---|---|---|
| 输入是什么 | 编码缓冲（`nihao`） | 刚上屏的文本（「你好」） |
| 谁来算 | `EngineManager::convert_with(schema, code)` | `wind_assoc::associate(ctx, cfg, providers)` |
| 有没有编码 | 有 | **没有** |
| 词库归属 | 有 | **没有** |

后两行是很多设计取舍的根源：联想候选没有编码、没有归属方案，于是**调频、调序、
删词、右键菜单一概不适用**——那些操作的 key 都要落到某个 `(schema, code)` 上。

## 2. 状态形状：联想 = 正常输入态，只是没有输入缓冲

**这是全篇最重要的一句，也是推翻过一版实现才得到的。**

联想候选就住在 `state.candidates` 里，`state.active` 仍是 `None`。于是主输入路的既有
能力**天然全部适用**——翻页、上下移高亮、二三候选键、数字选词、空格选高亮、Esc 取消、
鼠标点选，一行接线都不用写，因为那些分支的门槛本来就只是「候选非空」。

### 被推翻的那一版：独立候选字段

第一版另立了 `assoc: Vec<Candidate>` + `assoc_selected`，理由是「塞进 `candidates` 会
改变全 crate 28 处『候选非空』判断的语义，那种规模的审查一定会漏」。

**担心没错，结论反了。** 那 28 处里绝大多数正是我们**希望**生效的：翻页要能翻、
选词要能选、鼠标要能点。独立字段换来的是展示要投影（`view_candidates`）、翻页要投影、
高亮要投影，外加一整套专用按键闸门——把一件事做成了两件，而且做出来的那套还比
原来的**少功能**（二三候选、翻页在联想态下全都按不出来）。

> 教训：**「复用会波及 N 处」不足以否决复用**，还得问那 N 处里有多少是「本就该生效」的。
> 只数风险不数收益，就会把一个免费继承整条链路的方案，换成一个要自己重铺一遍的方案。

真正需要区分的只有一件事：**凡以「输入码」为 key 的加工必须跳过联想候选**——词频记账、
自动造词、码表调序，联想没有码。这用一个来源标记 `CandidateSource::Assoc` 就够了，
与短语「有文本无码位、恒不记词频」是同一个先例。判据也因此**不会与候选本身失步**：

```rust
pub(crate) fn assoc_active(&self) -> bool {
    self.candidates.first().is_some_and(|c| c.source == CandidateSource::Assoc)
}
```

### 也不是 overlay 模式

`ModeKind::Special/TempPinyin/Mix` 那套各有独立缓冲与专属处理器，联想没有缓冲、
也不需要专属语义，套进去同样是多一层。

## 3. 只剩几处需要主动接

| # | 位置 | 为什么非接不可 |
|---|---|---|
| 1 | 上屏路径末尾（`commit_selected` 整串分支） | 生成候选 + 开占位组合 |
| 2 | 退格 / 回车（`assoc_backspace` / `assoc_enter`） | 既有分支在「缓冲空 + 无已转换段」时给 `PassThrough`，会把占位组合悬在宿主里 |
| 3 | `try_activate_mode` 让位 | 见 §3.3 |
| 4 | 按键唯一出口的透传改判（`assoc_release_on_passthrough`） | 第 2 行那个病灶的**其余全部**键（Del / Home / End / ← → / Insert…），见 §3.3.1 |
| 5 | `try_activate_mode` 的临英 Shift+字母门 | 联想候选会把「无候选」那道门挡死，见 §3.3.2 |

（其余「接线」都随候选自然生效：候选被清空/重填时联想就结束了，无需显式退出。）

⚠️ **这张表不等于「占位组合已被全部收口」**，已知缺口一处（2026-09-07 审查发现，
尚未修）：语言栏中/英按钮（`CMD_TOGGLE_MODE`）与系统 Ctrl+Space
（`CMD_SYSTEM_MODE_SWITCH`）在联想态下 `take_input_on_mode_switch` 拿不到文本 ⇒
bridge 回 `StatusUpdate`，而 C++ 的 StatusUpdate 分支**明确不结束组合**，占位空格
悬着。对照组是 Shift 单击那条 keyup 路径——它的 `had_pending` 把
`!candidates.is_empty()` 算进去了，于是回空文本 `InsertText` 顺带 `EndComposition`。
修法与超时孤儿同源（`push_end_composition`，零协议改动）。

### 3.1 触发点为什么选在那里

`commit_selected` 的整串分支是**六类选词触发的唯一汇合处**（空格 / 数字键 /
次三选键 / 修饰键选词 / 鼠标点选 / 数字键越界 overflow），紧邻上方的「英文补空格」
正是因为同一个理由接在这里。

上文取的是 `out`（转换后的文本）而非 `final_simplified`：用户屏幕上真正出现的是
简繁转换、英文补空格之后的形态，联想该看的是**那个**。拿转换前的形态去联想，
繁体用户会在「你好」后面收到按简体规则算出来的推荐。

### 3.2 ★★★ 会话闸门：踩了两次的同一个坑

会话闸门回答的是「这个键要不要转发给输入法」。**它有两份独立实现**，两份都要接。

#### 第一层：`key_gate.rs`（Rust）

`has_input_session` 的注释里写着：

> 判据必须含候选：顶码/**联想**等场景下缓冲已清空但候选仍在

读起来像是这一层已经替联想想好了。**这个推断是错的**——那句话的前提是「候选仍在
`candidates` 里」，而联想候选存在 `assoc`。少了 `|| state.assoc_active()`，闸门在
联想态返回 false。

教训：**「注释提到了我要做的这件事」不等于「代码已经覆盖了它」**。那句注释写于本
功能存在之前，作者在描述一个假想场景，而假想里的联想候选放在哪儿，与实现无关。

#### 第二层：C++ `OnTestKeyDown`（桌面实际走的那一份）

补完第一层之后真机上退格**依然**不关联想窗。因为 `key_gate.rs` 开头就写着：

> C++ TSF：**尚未迁移**（保持现状，桌面零风险）

桌面走的是 C++ 那份独立实现，它有自己的镜像 `hasComposition || _hasCandidates`，
而普通上屏把两个标志都清零——这对普通上屏是对的，联想是唯一的例外。

第一版修法是 `CommitText` 响应加 **flags bit4**（`COMMIT_FLAG_KEEP_CANDIDATES`）：
composition 照常结束，只让 `_hasCandidates` 保持为真。

⛔ **这一版已废弃**（`BinaryProtocol.h` 里那一位标了「勿复用」）。它**仍然失败**：
`_hasCandidates` 由服务端应答**异步**回填，赢不了下一次 `OnTestKeyDown` 的竞速。
真机日志同一行里 `composing=0 candidates=1 inputSession=0`——判定取的是 0，日志打
出来已经是 1。

#### 第三步（现行）：挂占位组合

`HasActiveComposition()` 是 TSF 组合对象的**同步**状态，没有那个竞速窗口；特殊模式 /
临拼 / 临英一直可靠，正是因为它们都挂着组合。于是联想态也挂一个占位组合
（`handle_assoc::ASSOC_COMPOSITION` = 空格 + 光标落在它前面）。

★★★ **代价必须连着记**：一旦挂了组合，`_HasInputSession()` 就恒为真，
**落回原有链路的键不再是「干净的 `PassThrough`」**——TSF 已经把它吃下并转发了，
服务端再回 `PassThrough` 就是「吃了再吐」翻转。这正是 §3.3.1 那一整类缺陷的来源，
也是本文档一度写反的地方（旧版基于 bit4 方案写着「因为没有组合，退格照样删宿主的
字」，据此推理会得到完全错误的结论）。

**这两层的形状完全一样**：一份判据、两处实现、注释声称已覆盖、漏了不报错。
往「缓冲空但候选在」这类新稳定态上加任何按键行为，都要把两层一起查。

### 3.3 按键：几乎全部自动可用

联想候选住在 `state.candidates` 里，故主输入路的既有分支原样适用：

| 键 | 谁在处理 | 结果 |
|---|---|---|
| 数字 1-9 / 0 | `handle_number_key_select` | 选中并上屏 |
| 空格 | VK_SPACE 臂（`!candidates.is_empty()`） | 上屏当前高亮 |
| `;` / `'` 二三候选 | select-key 消费点 | 选第 2/3 条 |
| ↑↓ / PgUp PgDn / `-` `=` | `apply_session_action` | 移高亮 / 翻页 |
| Esc | `cancel_session` | 收窗 + 结束组合 |
| 字母 | `update_candidates` | 换成新一轮正常候选（**Shift+字母除外**，见 §3.3.2） |
| 标点 | 标点流水线 | 出标点、结束组合 |
| 鼠标点选 | `select_candidate_at` | 与键盘同一条 `commit_selected` |

**例外有四类**（与 §3 表格的第 2~5 行一一对应）：

- **退格 / 回车**（§3 表格第 2 行）。各有一个开关（`backspace_cancels_only` 默认吃键、
  `enter_cancels_only` 默认透传），默认相反是刻意的：回车透传是「把正事办了」，
  退格透传却是**删掉刚上屏的字**，不可逆。
- **落到透传的其余键**（Del / Home / End / ← → / Insert…）。
  收口在 `handle_assoc::assoc_release_on_passthrough`，见下方 §3.3.1。
- **`try_activate_mode` 让位**。它刻意「不要求候选为空」（为空码补全定的裁决），于是
  `;` 在联想态会去激活快捷输入而不是选第 2 条——联想窗被顶掉换成模式引导符，
  二三候选键**永远按不出来**。加一句 `if state.assoc_active() { return None }`。
  判据用 `assoc_active()` 而非「候选非空」，正是为了不动空码补全那条既有行为。
  ⚠️ **这一句的位置在函数中段，不在开头**：放开头会把同一函数里的「临英 Shift+字母」
  分支一起挡掉（见下一条）。
- **临英 Shift+字母的那道门**。它原先要求「无候选」，联想候选同样会把它挡死，
  于是联想窗一弹出来 Shift+字母就进不了英文。见 §3.3.2。

#### 3.3.1 落到透传的键：收组合 + 重放，顺带收窗

上面那张表覆盖的是「有人接住」的键。剩下一大类**没人接**：它们各自分支的门槛都是
「缓冲或已转换段非空」，而联想两者皆空 ⇒ 一路落到 `PassThrough`。

而联想态在宿主里挂着占位组合（§3.2 第三步），TSF 的 `_HasInputSession()` 因此为真 ⇒
`OnTestKeyDown` 把 Del / Home / End / ← →（C++ 归 `HotkeyType::CursorKey`）全吃下转发。
回一个 `PassThrough` 就是「吃了再吐」翻转：**不补发 `WM_KEYDOWN` 的宿主直接丢键**
（用户报的「退格能删，Del/Home/End 按了没反应」），补发的宿主则把占位空格 finalize
进文档。分界线全在宿主侧，我们这边是同一条路径。

修法与超时孤儿那条同形（`handle_assoc::adopt_orphaned_placeholder`，本文未展开）：
在按键处理的**唯一出口** `handle_key_event_policed` 上，
按「这个动作是不是把键交还宿主却不碰组合」改判成
`ClearCompositionThenPassThrough`，并退出联想态。

★ **判据取动作，不取键名**。「哪些键会落到透传」数不清——← →/Home/End 一条臂、
Del 一条臂、Insert 落兜底臂、Ctrl 组合另有一条；本仓已多次栽在「N 条通路只接了
N-1 条」上。而 `PassThrough` / `NotHandled` 这一格，问一次就够。

★ **顺带收窗不是副作用，是正解**：这批键要么移动宿主光标、要么改动光标后的文档，
联想赖以成立的上文已经断了，那批候选此刻就是错的。

⛔ **不给它配开关**。退格/回车有开关，是因为那两个键「只收窗、吃掉这一键」也说得通；
而「按 Home 只收窗、光标不动」对不上任何一种用户期待——那不是取舍，是坏掉。

⚠️ **修饰键本身必须排除**（判据 `handle_assoc::is_modifier_like_vk`）：Shift+字母的
物理形态是「先 Shift keydown、再字母 keydown」，不排除的话第一下就把联想窗收掉了，
§3.3.2 修好的临英入口会被自己撞掉。
★ 判据**就地写全**（含左右 Alt 与未拆左右的笼统 VK），不复用 `is_key_up_only_vk`：
那个答的是「这键只有 keyup 到得了服务端」，是另一根轴。两轴今天在桌面上答案重合，
但这道守卫自称是「给薄宿主的保险」，而薄宿主正是最可能上报 Alt keydown 的那一类。

★ **Tab 不在此列**。它在 C++ 侧同样「有 session 就吃」，但在协调器里归辅助码触发键
（`enter_aux_code`），给的是 `Consumed` 而非透传——用户看到的同样是「按了没反应」，
但机制不同（键被干净吃掉、不丢键）。要不要让它在联想态做点别的是另一个问题。

#### 3.3.2 Shift+字母进临时英文

`try_activate_mode` 里的临英分支原先要求 `candidates.is_empty()`，加上函数开头那句
联想让位，两道门各自都足以把联想态挡在外面 ⇒ 联想窗一弹出来，Shift+字母就进不了临英，
字母径直落进中文码表缓冲：**用户想打英文，得到的是中文输入**。

两处一起改：让位挪到临英分支之后（让位保的是 `;`/`'` 这类**选词键**，与 Shift+字母的
值域不相交），门放宽成 `candidates.is_empty() || assoc_active()`。

⚠️ 判据仍**不是**「候选非空」——空码补全那类候选是用户真在看的，行为一字不动。
★ 进模式前先 `exit_assoc`：联想候选与临英候选住的是同一个 `state.candidates`，
不先清就会出现「临英只有一条候选、下面还挂着上一轮的联想词」；顺带作废未触发的自动
隐藏计时，否则它会在用户已经打着英文时到期、把占位组合标成孤儿。

### 3.4 自动隐藏与编码栏标识

联想态下候选窗几乎一直挂着，长时间停留会挡正文。`hide_after_ms`（默认 5000，
`0` = 不隐藏）到期即收窗。

用的是**独立的单槽 timer**，不与首显那个共用：联想态与首显等待会同时存在（刚上屏时
候选窗正等宿主 reflow 后的权威坐标，此刻也正要起自动隐藏计时），共用一个槽等于互相
取消——表现为「联想窗有时压根不出现」或「有时永远不消失」，且取决于两次 arm 的先后。

★ **进入与退出联想态都要 bump 令牌**。退出时不 bump，旧计时会在下一轮联想里提前把窗
收掉：现象是「有时联想刚出来就没了」，只在两次上屏间隔短于 `hide_after_ms` 时复现。

编码栏标识（`hint`，默认「联想输入」）借用 `state.preedit`——它平时装的是真实编码，
所以清理必须跟着联想态走，漏了就会留到下一次打字。联想态强制
`preedit_host_owned = false`：此刻宿主侧**没有 composition**，没有地方可嵌；而那四个字
真流进宿主就是写进用户的文档。因此标识只在「编码不嵌入宿主」时可见——嵌入模式下
编码栏本身不存在。

## 4. 四个数据源与它们的排序

| 源 | 数据来源 | 状态 | word 档 | smart 档 |
|---|---|---|---|---|
| `History` | 用户上屏历史（redb） | ⬜ 待做，需新建搭配表 | — | ✔ |
| `Bigram` | 词→后继表 | ⬜ 待做，需离线蒸馏 | — | ✔ |
| `Prefix` | 词库里以上文为前缀的更长的词 | ✅ 已实现 | ✔ **唯一** | ✔ |
| `Punct` | 静态规则表 | ✅ 已实现 | ✖ **禁止** | ✔ |

**档位过滤先于用户开关**（`AssocConfig::source_enabled`）。顺序反过来就会出现
「词语联想 + `punct = true` 出标点」——那正是要修的问题。用户开关只在档位放行的
源上生效，故 `word` 档下 `punct = true` 是无害的死配置。

**跨源不比较分数，只按固定优先级取配额。** 四个源的分值天然不同量纲（计数 /
log 概率 / 词频 i32 / 规则无分），归一化混排是条老路——`freq-rerank-model` 记着
整句权重那次的教训：同量纲解决不了系统性偏高，最后还是拆成了独立档位。

配额不是平均分：靠前的源先取，取不满的名额**顺延**给后面的源，否则个人历史为空的
新用户会白白浪费掉最靠前的几个位置。被去重吃掉的名额同样顺延（按「已产出数」而非
「已请求数」算剩余）。

`History` / `Bigram` 尚无 provider ⇒ `associate` 对它们 `continue`，配额自动顺延。
这是**有意的分期，不是漏接**。

### 4.0 Prefix 源：怎么做到不加索引就能按前缀取词

反查索引（`ReverseIndex`，词 → 编码，供悬停 [编码] 段用）本就**按 text 字节序升序**存
——那是二分查码的前提。字节序下同前缀的词必然连续，于是前缀扫描 = 二分找下界 +
顺序走到第一个不匹配为止，**不需要任何新索引**。

唯一缺的是权重：不带权重就只能给字典序前几个（「中一」「中丁」…），那不是任何人
想要的。给每条加了 4 字节 `weight`（同词多码取最大值），十万词级约 400 KB。

★ **前缀词自己必须排除**——它的权重恰恰常常是全场最高（单字比词常用），不排除会稳定
占据首位，而选中它等于上屏一个空串。

★ **不做提前截断**：按权重取 top-N 必须看完全部候选，扫到一半就停会退化成「字典序前
N 里权重最高的那几个」——那是个看起来对、实则完全不对的结果。「中」开头约数千词，
每次上屏扫一遍是微秒级。

真实词库实测（`build_dev/pinyin`，探针 `assoc_prefix_words.rs`）：

```text
中   → 中国 中的 中间 中国人 中文 中心 中医 中午
北京 → 北京的 北京大学 北京市 北京时间 北京人 北京奥运会
输入 → 输入法 输入输出 输入框 输入密码 输入数据
谢   → 谢邀 谢谢 谢谢大家 谢霆锋 谢谢你
```

**显示整词、上屏只补剩余部分**：候选栏显示「中国」（用户才看得懂自己在选什么），
而「中」已经在屏幕上了，真正补出去的只有「国」。这一半存在 `AssocHit::commit` /
`Candidate::commit_override`，**不由下游按前缀现算**——smart 档的候选不以上文开头，
下游若统一 `strip_prefix` 会把两档语义搅在一起，而 strip 失败时的兜底又恰好看起来
是对的，这类缺陷不报错、只偶尔少个字。

⚠️ `commit_override` 与 `s2t_override` **不是一回事，别互相顶替**：后者会绕过出口的
`maybe_s2t`，拿它装联想后缀会让开着简繁的用户补出来的那半截不转换。

### 4.1 标点为什么只能是规则表（仅 smart 档）

主流移动端输入法的标点联想是**把标点当普通 token 训进语言模型**的——「你好」后面
出「，」是因为语料里 `你好 ，` 的 bigram 频次高，而不是有人写了张表。那样更细腻：
「吗→？」「谢谢→！」这类区分是规则表写不出来的。

但我们手上的模型都做不到。实测（2026-08-15）三份 `.gram` **全部不含任何标点、
数字、拉丁字符**，只有汉字：

```text
zh-hans-bgc / zh-hans-bgw / wanxiang-lts-zh-hans
  「，」「。」「？」「！」「、」「；」「：」  → traverse 全部 FAIL
  数字 0-9、字母 A-Z                        → traverse 全部 FAIL
```

中文语料在分词前普遍剥掉标点，这三份都不例外。

⚠️ **将来若自建语料，预处理必须显式保留标点 token**——这与业界默认的清洗行为相反，
不写下来一定会被下一个人默默「清洗」掉。

### 4.2 规则表的内容

- 汉字结尾 → `，。、？！：；`（逗号最优先；问号叹号靠后，它们依赖语气，规则表判断
  不了，放前面会经常碍事）
- 数字结尾 → `年月日元个点%℃`。★ **数字后推荐单位而非标点**——「2026」后面用户想要
  的是「年」。触屏切到符号键盘打一个「年」很贵。

  ⚠️ **这条在桌面走不到**：PC 上数字键在空缓冲时直接透传给宿主，不经 IME 的上屏路径，
  于是 `commit_selected` 根本不触发。它只在移动端生效（那里数字也走 IME）。刻意不在
  桌面的数字透传点补触发——用户每打一个数字都弹联想窗，干扰远大于收益。
- 标点结尾 → **不联想**（否则会出现「。」后面接「，」）
- 拉丁字母 / 空白结尾 → 不联想（英文有自己的输入习惯，别插手）

## 5. 配置

```toml
[input.association]      # 桌面基线
kind = "off"            # off / word / smart —— 兼任开关
mode = "one_shot"       # one_shot / continuous
max_count = 9
space_commits = true    # 空格是否上屏当前高亮
hide_after_ms = 5000    # 联想窗自动隐藏；0 = 不隐藏
hint = "联想输入"        # 编码栏标识；空串 = 不显示
history = true
bigram = true
prefix = true
punct = false

[mobile.association]     # 移动端取值（桌面构建完全无视本域）
kind = "smart"
mode = "continuous"
punct = true
```

★ **`kind` 兼任开关与类型**。拆成 `enabled` + `kind` 两个字段会立刻产生「开着但类型
没配」的歧义状态，而那个状态没有正确答案。

**桌面默认关**：联想会占用数字键——上屏后再按数字，开着时是选联想词、关着时是打数字。
对既有用户这是突发的行为变化，必须显式打开。

### 5.1 平台差异走 `[mobile]` 域，不进值域

三个键在两个平台上取值不同：

| | 桌面 | 移动端 | 理由 |
|---|---|---|---|
| `kind` | `off` | `smart` | 软键盘上每多打一个字都很贵，连标点带下一个词一起猜才划算 |
| `mode` | `one_shot` | `continuous` | 候选窗是浮在正文上的窗口，常驻会挡字；软键盘上方的联想栏本就常驻 |
| `punct` | `false` | `true` | 实体键盘上标点一键可达；软键盘上要切键盘层 |

`[mobile]` 是**顶层域**，桌面构建读都不读。**只登记当前真有平台差异的键**——本域里
出现的键移动端就用这里的值，没出现的沿用基线。别提前铺一排永远等于基线的键，每个都
要在 REGISTRY、预置文件、capability 快照、设置页豁免名单里各占一行。

整个联想只有一处平台判断：协调器的 `use_mobile_overrides()`。`wind-assoc` 自己不知道
跑在哪，取不取用移动端段由调用方传 `Option` 决定。

⛔ **不要改用「值域哨兵」**（让 `kind` 取 `"auto"`、由宿主按平台解释）。那样平台知识
就进了值域，后果有两层：设置界面被迫列一个语义空洞的「自动」选项，用户看不出选了会
得到什么；core 侧还要为每个字段维护一份 `platform_default_*`，并让解析函数多收一个
「平台默认」参数——平台知识会顺着解析链一路渗透。

⛔ **也不要靠「两份预置文件各写各的」**。安卓仓 `assets/data/config.toml` 确实是独立
的一份，但它是**手工副本且无守门测试**（实测滞后过 89 行）。把平台差异寄托在「两份
文件恰好不一样」上，下次同步就被覆盖回去了。

★ 移动端段的字段用 `String` 而非 `Option<String>`：`Option` 更贴「覆盖」的字面意思，
但 `None` + `skip_serializing_if` 会让本段在默认配置里序列化成**空表**，于是
`mobile.association` 自己成了叶子键，三道注册表守门测试全要为它开特例。而那份「稀疏」
能力本就多余——只登记有差异的键时，「跟基线」的情形不存在。

## 6. 持续档的收敛

`continuous` 选中一条后会拿刚上屏的那条当新上文再联想一轮。它靠规则表天然收敛：
「你好」→ 选「，」→ 标点结尾不再联想 → 自然收窗。不是靠计数器限制轮数。

守门测试是一对：`continuous_mode_really_chains`（「2026」→ 选「年」→ 汉字结尾，
续上一轮）与 `continuous_mode_chains_then_settles`（选标点后收窗）。少了前者，
「持续档其实和一次性档一样」这个缺陷测不出来。

## 7. 测试策略

`wind-assoc` 的单元测试只证明「给定上文能算出候选」。**联想真正会坏的地方全在接线
上**——闸门没接、展示没接、按键被别的分支先消费掉。故 `handle_assoc::wiring_tests`
一律从 `handle_key_event` 进，与用户入口同层。

两条最关键的接线各做过**摘除验证**，确认摘掉会红：

- `assoc_state_counts_as_active_session` ← 摘掉 `has_input_session` 里的 `assoc_active()`
- `assoc_candidates_reach_the_candidate_window` ← 把 `view_candidates` 改成恒返回 `candidates`

headless 下没有宿主上报 caret，每一帧都是「首帧且坐标未就绪」，`notify_ui_update`
会在首显闸门处 return——断言「UI 收到了什么」的测试必须先调
`debug_mark_coords_ready()`，否则拿到空通道，看着像功能没接上。

## 8. 待做

- `History` 源：redb 搭配表（`(上文词, 下一个词) → 计数`）
- `Bigram` 源：从 `.gram` 离线蒸馏词→后继表（模型本身太大，不适合在线查后继）
- 词语联想的**降噪**：真实词库里「中的」「我的」「工作了」这类「实词 + 虚词」会挤进
  前排（词频确实高）。主流输入法多半有一张虚词尾表在过滤。待真机用一段时间再定
  ——现在就加规则，等于在没有观察数据的情况下调参。
- 移动端接线：`AssocContext::text` 改由 `InputConnection.getTextBeforeCursor` 填入
  光标前的真实内容。一期就按「完整上文」设计接口，正是为了那时不必改签名。
- 设置页：`wind-setting` 仓、文档站、工具站三处的配置项同步
